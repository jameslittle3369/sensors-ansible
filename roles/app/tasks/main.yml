---
# Placeholder role for rpi4-app (Grafana + uv-managed Python + standalone
# scripts run as systemd services/timers). No implementation yet -- see
# roles/postgres for the task-file-per-concern pattern to follow when this
# is built out.
- name: (stub) rpi4-app role not yet implemented
  ansible.builtin.debug:
    msg: "roles/app has no tasks yet -- placeholder only."
