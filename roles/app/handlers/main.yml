---
- name: reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart rtl433-postgres
  ansible.builtin.systemd:
    name: rtl433-postgres
    state: restarted
    daemon_reload: true
